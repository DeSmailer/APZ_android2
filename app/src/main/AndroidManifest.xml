<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pyliavskyi.apz_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.APZ_android">
        <activity android:name=".ChatActivity"></activity>
        <activity android:name=".MyChatsActivity" />
        <activity android:name=".MyInstitutionsActivity" />
        <activity android:name=".AllInstitutionActivity" />
        <activity android:name=".ChatCodeFieldActivity" />
        <activity android:name=".InstitutionProfileActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>